#include "sam_helper.h"

int main(int argc, char ** argv)
{
    SamLine samline;

    std::map
    while (samline.load(stdin))
    {
        if (samline.is_header)
        {
            continue;
        }
        
    }
    return 0;
}
