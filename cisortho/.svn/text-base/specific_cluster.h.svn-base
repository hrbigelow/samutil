/*
class specific_cluster : public cluster {

	set<hit_orientation, std::less<hit_orientation> > allowed_relations;
	vector<vector<gap_range> > dist_range;

 public:
	specific_cluster(vector<scoring *> const&, char const*);

	range neighbor(RIT_M_R, RIT_M_R);
};
*/
